name: Validate CLI and MCP Server

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    # Runs at 00:00 UTC every day (approx. 4 PM or 5 PM PDT)
    - cron: '0 0 * * *'

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run PEP8 Check
        run: |
          pip install flake8
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics

  test-mcp-and-cli:
    runs-on: ubuntu-latest
    needs: lint
    steps:
    - name: Checkout Code
      uses: actions/checkout@v5
    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.12'
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        # Ensure MCP and Playwright dependencies are explicitly ready
        pip install playwright playwright-stealth fastmcp
        playwright install chromium --with-deps
    - name: Validate CLI Tool (Scraping Test)
      run: |
        # Simulate a location input and verify that a price ($) is found in the output
        printf "San Jose, CA\n" | python3 gas_tool.py | grep "\$"
      env:
        PYTHONPATH: .
    - name: Validate MCP Server (Smoke Test)
      run: |
        # Launches the server to verify imports and FastMCP initialization.
        # timeout 15s ensures we don't hang.
        # [ $? -eq 124 ] treats the timeout (success) as a passed test.
        timeout 15s python3 gas_server.py || [ $? -eq 124 ]
      env:
        PYTHONPATH: .
